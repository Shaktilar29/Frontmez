export type State = {
    loading: boolean;
    showCreateModal: boolean;
    showEditModal: false;
  };

  export const state: State = {
    loading: false,
    showCreateModal: false,
    showEditModal: false,
  };